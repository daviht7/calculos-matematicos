# Analise

- A solucao possui uma biblioteca .NET 8 e testes xUnit.
- A branch publicada da demanda anterior estava disponivel.
- Foi criada `staging` a partir do commit anterior e publicada no remoto.
- A nova feature foi iniciada a partir de `staging`.
- A API existente usa `decimal` para operacoes aritmeticas; a media segue o mesmo contrato.

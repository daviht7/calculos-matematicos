# Analise

- A solucao possui uma biblioteca .NET 8 com a classe estatica `Calculadora`.
- Os testes utilizam xUnit.
- O PR anterior foi incorporado em `staging` antes desta demanda.
- O tipo `decimal` foi mantido por consistencia com os metodos de comparacao existentes.

# Analise

- O projeto possui uma biblioteca .NET 8 com a classe estatica `Calculadora`.
- Os testes utilizam xUnit.
- A branch `staging` esta publicada e foi atualizada antes do desenvolvimento.
- A nova branch foi criada a partir de `staging`.
- O tipo `decimal` foi mantido por consistencia com as operacoes aritmeticas existentes.
